#This file is generated by updateDaemonJvm
# 项目源级别是 21；让 wrapper 自动在本机已安装的 JDK 里挑 21 启动守护进程，
# 不再依赖 JAVA_HOME 指向哪一版（JAVA_HOME=17 时 :aidl:compileDebugJavaWithJavac 会报"无效的源发行版 21"）。
# 不配下载地址：机器上没有 JDK 21 时直接报"找不到匹配的 JDK"，而不是静默联网下载。
toolchainVersion=21
